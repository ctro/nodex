chai = require('chai');

describe("A Fake test", function() {
  it("makes fake assertions", function() {
      chai.assert(true);
  });
  it("makes fake assertions", function() {
      chai.assert(true);
  });
  it("makes fake assertions", function() {
      chai.assert(true);
  });
  it("makes fake assertions", function() {
      chai.assert(true);
  });
  it("makes fake assertions", function() {
      chai.assert(true);
  });
  it("makes fake assertions", function() {
      chai.assert(true);
  });
  it("makes fake assertions", function() {
      chai.assert(true);
  });
  it("makes fake assertions", function() {
      chai.assert(true);
  });
  it("makes fake assertions", function() {
      chai.assert(true);
  });
  it("makes fake assertions", function() {
      chai.assert(true);
  });
  it("makes fake assertions", function() {
      chai.assert(true);
  });
  it("makes fake assertions", function() {
      chai.assert(true);
  });
  it("makes fake assertions", function() {
      chai.assert(true);
  });
});
